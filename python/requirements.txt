# Core CSV handling and validation
pandas>=2.0.0
python-dateutil>=2.8.2

# For Markdown rendering and table checks (no CSV dialect changes)
tabulate>=0.9.0

# CLI ergonomics (optional but useful)
click>=8.1.0
